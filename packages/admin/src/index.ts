/**
 * @csp/admin — the one shared admin app served to every tenant.
 *
 * Becomes a React + Vite app in issue #18 (block-list editor rendering shared EditForms),
 * #19 (iframe live preview over postMessage) and #20 (presigned image upload). Stubbed for now.
 */
export const ADMIN_PACKAGE = '@csp/admin';
